-r requirements.txt

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
# httpx is already a runtime dep; ASGITransport lives there too.

# Linting / formatting
ruff==0.6.9

# Stress test harness
matplotlib==3.9.2
pandas==2.2.3
